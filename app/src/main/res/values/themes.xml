<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Transparent window background — Compose draws everything -->
    <style name="Theme.GooseAndroid" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
